'use client';

// This component is no longer needed as streaming is handled locally in CounterfactualExperimentViewer
// Keeping as empty component for backward compatibility
export default function ExperimentStreamManager({
  workspaceId,
}: {
  workspaceId: string;
}) {
  return null;
}
